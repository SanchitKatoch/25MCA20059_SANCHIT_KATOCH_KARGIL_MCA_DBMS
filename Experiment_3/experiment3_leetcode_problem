https://leetcode.com/problems/bank-account-summary-ii/submissions/1898866994/

query solution:- 

SELECT name,SUM(Transactions.amount) AS balance FROM Users
JOIN Transactions ON Users.account = Transactions.account
GROUP BY Users.account,Users.name HAVING SUM(Transactions.amount) > 10000;
